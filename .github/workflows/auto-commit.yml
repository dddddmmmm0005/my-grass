name: Auto Commit Every 5 Days

on:
  schedule:
    # 5일마다 실행 (UTC 기준이라 한국시간으로는 +9시간)
    - cron: "0 0 */5 * *"
  workflow_dispatch:  # 수동 실행도 가능하게

jobs:
  auto-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Update README
        run: |
          echo "🌿 자동 커밋: $(date '+%Y-%m-%d %H:%M:%S')" >> README.md

      - name: Commit and push changes
        run: |
          git config --global user.name "dddddmmmm0005"
          git config --global user.email "dmsgh051203@gmail.com"
          git add README.md
          git commit -m "자동 잔디 커밋 🌱"
          git push
